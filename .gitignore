# Secrets — Alpaca API keys etc.
.env
.env.local
.env.*

# SSH / private keys — must NEVER be committed (this repo is public).
# Keys live in ~/.ssh, not here; this is belt-and-suspenders against an accident.
*.key
*.pem
*.ppk
id_rsa
id_ed25519
id_*

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.ruff_cache/

# Virtualenvs
.venv/
.venv-openbb/

# uv
.uv-cache/

# Claude Code local-only settings (project settings.json IS shareable; .local.json is not)
.claude/settings.local.json

# Runtime artifacts
logs/
backtest/orb_trades.csv
# dot-prefixed .pkl in backtest = regenerable data cache (bars, pit, swing, put-spread, daily-base)
backtest/.*.pkl
backtest/OVERNIGHT_RESULTS.md
backtest/overnight.sh
experiments/news_edge/wake_log.txt
# lottery experiment runtime state/caches (regenerable; mutate daily/weekly)
experiments/lottery/.uoa_state.json
experiments/lottery/.short_interest_cache.json
logs/lottery_positions.json
*.tmp
openbb-out.json

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# PEAD research event table (regenerable via backtest/run_pead.py)
backtest/pead_events.csv

# Strategies summary (regenerable via scripts/generate_strategies_summary.py)
strategies_summary.xlsx

# Scanner outputs (regenerable via scripts/swing_screener.py / institutional_check.py / accumulation_scan.py)
scripts/swing_screen_*.csv
scripts/institutional_check.csv
scripts/accumulation_scan.csv
scripts/accumulation_chatter_*.csv
# Insider-buy event log (regenerable from EDGAR Form 4s; collected by scripts/insider_collect.py)
scripts/insider_buys_log.csv
# Biotech surge radar (regenerable via scripts/biotech_radar.py; universe from XBI holdings)
scripts/biotech_radar_*.csv
scripts/.biotech_universe.txt

# Claude scheduled-tasks lock (transient harness artifact)
.claude/scheduled_tasks.lock

# generated intermediate for factor_exposure.py
backtest/.orb_daily_pnl_*.csv
